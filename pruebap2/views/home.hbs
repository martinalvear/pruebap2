<h1>Lista de Pokémon</h1>
<a href="/update-stock" class="btn btn-secondary">Actualizar Stock desde CSV</a>
<table>
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Imagen</th>
            <th>Cantidad</th>
        </tr>
    </thead>
    <tbody>
        {{#each pokemons}}
            <tr>
                <td>{{this.name}}</td>
                <td><img src="{{this.url}}" alt="{{this.name}}" style="width: 50px; height: 50px;"></td>
                <td>{{this.cantidad}}</td>
            </tr>
        {{/each}}
    </tbody>
</table>
